# Rust build artifacts
ai-native/core/target/
ai-native/core/Cargo.lock

# macOS
.DS_Store

# Models and downloaded binaries (too large for git)
*.gguf
*.zip

# Finetune directory (training scripts, datasets, model weights)
finetune/

# Demo recordings
demo.tape
demo_sim.sh
